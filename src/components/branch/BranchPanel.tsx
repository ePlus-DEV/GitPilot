import { useGitStore } from '../../store/gitStore';import { gitService } from '../../services/gitService';export function BranchPanel(){const s=useGitStore();const repo=s.repo?.path;return <div className="p-3"><h3>Branches</h3>{s.branches.filter(b=>!b.remote).map(b=><button className="btn m-1" key={b.name} onClick={()=>repo&&s.run('compare branch',()=>gitService.compareBranch(repo,b.name))}>{b.name}</button>)}</div>}
