import Editor from '@monaco-editor/react';import { useGitStore } from '../../store/gitStore';export function DiffViewer(){const{diff,selectedFile}=useGitStore();return <div className="min-h-0"><div className="px-3 py-2 border-b border-pilot-line text-sm">Diff {selectedFile?.path}</div><Editor theme="vs-dark" language="diff" value={diff||'// Select a file to inspect its staged or unstaged diff'} options={{readOnly:true,minimap:{enabled:false},fontSize:13}}/></div>}
