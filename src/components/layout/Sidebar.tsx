import { useState } from 'react';import { useGitStore } from '../../store/gitStore';export function Sidebar(){const[path,setPath]=useState('');const{setRepositoryPath,branches}=useGitStore();return <aside className="p-3 space-y-4 bg-[#0d1324] overflow-auto"><section><label className="text-xs uppercase text-slate-400">Repository path</label><div className="flex gap-2 mt-2"><input value={path} onChange={e=>setPath(e.target.value)} placeholder="/path/to/repo" className="min-w-0 flex-1 bg-slate-900 border border-pilot-line rounded px-2 py-1"/><button onClick={()=>setRepositoryPath(path)} className="px-2 rounded bg-pilot-blue text-slate-950">Open</button></div></section><section><h2 className="text-sm font-semibold mb-2">Branches</h2>{branches.map(b=><div key={b.fullName} className={`text-sm px-2 py-1 rounded ${b.current?'bg-pilot-purple/30 text-white':'text-slate-300'}`}>{b.current?'● ':''}{b.name}{b.remote?' ↗':''}</div>)}</section></aside>}
