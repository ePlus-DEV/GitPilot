import { useGitStore } from '../../store/gitStore';export function TopBar(){const{repository,refresh,loading}=useGitStore();return <header className="flex items-center gap-3 px-4 border-b border-pilot-line bg-pilot-panel"><div className="font-bold text-lg bg-gradient-to-r from-pilot-blue to-pilot-purple text-transparent bg-clip-text">GitPilot</div><div className="text-sm text-slate-300 truncate flex-1">{repository?`${repository.name} — ${repository.path}`:'Open a Git repository to begin'}</div><span className="rounded bg-slate-800 px-2 py-1 text-xs">{repository?.currentBranch??'no branch'}</span><button onClick={refresh} className="px-3 py-1 rounded bg-slate-800 hover:bg-slate-700" disabled={loading}>Refresh</button><button className="px-3 py-1 rounded bg-pilot-purple">Fetch</button><button className="px-3 py-1 rounded bg-pilot-blue text-slate-950">Push</button></header>}
