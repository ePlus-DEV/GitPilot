import { useGitStore } from '../../store/gitStore';import { gitService } from '../../services/gitService';export function RebasePanel(){const s=useGitStore();const repo=s.repo?.path;return <div className="p-3"><h3>Rebase</h3>{s.branches.map(b=><button className="btn m-1" key={b.name} onClick={()=>repo&&s.run('rebase',()=>gitService.startRebase(repo,b.name))}>onto {b.name}</button>)}<button className="btn" onClick={()=>repo&&s.run('continue rebase',()=>gitService.continueRebase(repo))}>Continue</button><button className="btn" onClick={()=>repo&&s.run('abort rebase',()=>gitService.abortRebase(repo))}>Abort</button><button className="btn" onClick={()=>repo&&s.run('skip rebase',()=>gitService.skipRebase(repo))}>Skip</button></div>}
