import { useGitStore } from '../../store/gitStore';
export function HistoryPanel(){const s=useGitStore();return <section className="min-h-0 flex-1 overflow-auto border-r border-pilot-line bg-[#0d1117]"><h2 className="sticky top-0 bg-[#0d1117] p-3 text-sm font-semibold">Commit History</h2>{s.history.map(c=><button key={c.hash} onClick={()=>void s.selectCommit(c)} className="flex w-full gap-2 border-t border-pilot-line p-2 text-left hover:bg-[#21262d]"><span className="w-12 whitespace-pre font-mono text-pilot-blue">{c.graph}</span><div className="min-w-0"><div className="truncate text-sm">{c.head?'HEAD ':''}{c.message}</div><div className="text-xs text-slate-500">{c.shortHash} · {c.author} · {c.date} {c.refs.join(' ')}</div></div></button>)}</section>}
