import { useGitStore } from '../../store/gitStore';import { gitService } from '../../services/gitService';export function RemotePanel(){const s=useGitStore();const repo=s.repo?.path;return <div className="p-3"><h3>Remote Operations</h3><button className="btn" onClick={()=>repo&&s.run('fetch',()=>gitService.fetch(repo))}>Fetch</button><button className="btn" onClick={()=>repo&&s.run('pull',()=>gitService.pull(repo))}>Pull</button><button className="btn" onClick={()=>repo&&s.run('push',()=>gitService.push(repo))}>Push</button></div>}
