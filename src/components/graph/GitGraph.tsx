import { useGitStore } from '../../store/gitStore';export function GitGraph(){const h=useGitStore(s=>s.history.slice(0,20));return <div className="border-b border-pilot-line p-3"><div className="mb-2 text-xs uppercase text-slate-500">Graph</div>{h.map(c=><div className="font-mono text-xs" key={c.hash}><span className="text-pilot-purple">{c.graph||'*'}</span> {c.shortHash} {c.message}</div>)}</div>}
