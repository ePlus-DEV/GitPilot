import { useGitStore } from '../../store/gitStore';export function ConsolePanel(){const lines=useGitStore(s=>s.console);return <footer className="border-t border-pilot-line bg-black/40 p-3 overflow-auto"><div className="text-xs uppercase text-slate-400 mb-2">Git Output · Validation · Problems · AI</div>{lines.map((l,i)=><pre key={i} className="text-xs text-slate-300 whitespace-pre-wrap">{l}</pre>)}</footer>}
