import { useGitStore } from '../../store/gitStore';import { gitService } from '../../services/gitService';export function ConsolePanel(){const s=useGitStore();const repo=s.repo?.path;return <footer className="h-44 border-t border-pilot-line bg-black"><div className="flex gap-2 border-b border-pilot-line px-3 py-1 text-xs"><button className="btn" onClick={()=>repo&&s.run('validation',()=>gitService.runValidation(repo))}>Run Validation</button><span>Git Output · Validation · Problems · AI</span></div><pre className="h-32 overflow-auto whitespace-pre-wrap p-3 text-xs text-slate-300">{s.console.join('\n\n')||'Git output console'}</pre></footer>}
